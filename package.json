{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:cli": "npm run dev --workspaces @kitimat/graphql-cli",
    "dev:plugin": "npm run dev --workspaces @kitimat/graphql-plugin",
    "test": "npm test -w @kitimat/graphql-cli",
    "build": "npm run build --workspaces"
  },
  "dependencies": {
    "@kitimat/graphql-plugin": "^1.0.0"
  }
}
