{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:cli": "npm run dev --workspaces some-graphql-cli",
    "dev:plugin": "npm run dev --workspaces some-graphql-plugin",
    "test": "npm test -w some-graphql-cli",
    "build": "npm run build --workspaces",
    "demo": "npm run generate -w some-graphql-demo"
  }
}
